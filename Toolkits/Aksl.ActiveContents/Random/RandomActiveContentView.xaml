<UserControl x:Class="Aksl.ActiveContents.Views.RandomActiveContentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ei="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:views="clr-namespace:Aksl.ActiveContents.Views"
             xmlns:viewmodels="clr-namespace:Aksl.ActiveContents.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <UserControl.Resources>
        
        <ItemsPanelTemplate x:Key="activeContentItemsPanelTemplate">
            <Grid></Grid>
        </ItemsPanelTemplate>

        <DataTemplate x:Key="activeContentDataTemplate"
                      DataType="{x:Type viewmodels:ActiveContentItemViewModel}">
            <views:ActiveContentItemView />
        </DataTemplate>
    </UserControl.Resources>

    <Grid Background="{Binding ActiveBackground, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}">
        <ItemsControl ItemsSource="{Binding Path=ActiveContentItems}"
                      ItemsPanel="{Binding Source={StaticResource activeContentItemsPanelTemplate}}"
                      ItemTemplate="{Binding Source={StaticResource activeContentDataTemplate}}">
        </ItemsControl>
    </Grid>

</UserControl>
