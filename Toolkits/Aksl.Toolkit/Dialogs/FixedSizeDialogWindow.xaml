<Window x:Class="Aksl.Dialogs.Views.FixedSizeDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        ResizeMode="NoResize"
        Title="{Binding Title}"
        WindowStartupLocation="CenterScreen"
        xmlns:views="clr-namespace:Aksl.Dialogs.Views">
    <Window.Style>
        <Style TargetType="{x:Type Window}" >
            <Setter Property="SizeToContent" Value="WidthAndHeight" />
        </Style>
    </Window.Style>
    <Border BorderThickness="1"
            CornerRadius="2">
        <Border.BorderBrush>
         <LinearGradientBrush StartPoint="0,0"
                              EndPoint="1,1"
                              MappingMode="RelativeToBoundingBox"
                              SpreadMethod="Pad">
             <LinearGradientBrush.GradientStops>
                 <GradientStop Color="Black"
                               Offset="0.0" />
                 <GradientStop Color="WhiteSmoke"
                               Offset="0.5" />
                 <GradientStop Color="Black"
                               Offset="1.0" />
             </LinearGradientBrush.GradientStops>
         </LinearGradientBrush>
     </Border.BorderBrush>
        <Grid x:Name="DialogLayoutRootGrid"
              SnapsToDevicePixels="True">
            <Grid.RowDefinitions>
                <RowDefinition Height="32" />
                <RowDefinition Height="2" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid Grid.Row="0"
                  Background="{StaticResource PrimaryThemeBrush}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="1" 
                           FontSize="15"
                           FontWeight="Black"
                           VerticalAlignment="Center"
                           Margin="20,5,0,0"
                           Text="{Binding Title,RelativeSource={RelativeSource Mode=FindAncestor,AncestorType={x:Type Window}}}"
                           Background="{Binding Path=(Grid.Background),RelativeSource={RelativeSource Mode=FindAncestor,AncestorType={x:Type Grid}}}"
                           Foreground="{Binding Path=(Window.Foreground),RelativeSource={RelativeSource Mode=FindAncestor,AncestorType={x:Type Window}}}">
                </TextBlock>

                <views:FixedSizeDialogWindowIconButtonView Grid.Column="2" />
            </Grid>

            <Separator Grid.Row="1"
                       Margin="0,0,0,0"
                       Height="8"
                       BorderBrush="#193441" />

            <ContentControl x:Name="ContentPair"
                            Grid.Row="2"
                            Margin="0">
                <ContentControl.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform />
                        <SkewTransform />
                        <RotateTransform />
                        <TranslateTransform />
                    </TransformGroup>
                </ContentControl.RenderTransform>
            </ContentControl>
        </Grid>
    </Border>
</Window>
