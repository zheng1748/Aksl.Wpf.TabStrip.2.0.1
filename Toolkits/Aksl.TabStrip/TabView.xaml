<UserControl x:Class="Aksl.TabStrip.Views.TabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:views="clr-namespace:Aksl.TabStrip.Views"
             xmlns:viewmodels="clr-namespace:Aksl.TabStrip.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450"
             d:DesignWidth="800">
    <UserControl.Resources>

        <CollectionViewSource x:Key="tabSource"
                              Source="{Binding Path=ActiveTabItems}" />

        <Style x:Key="tabItemStyle"
               TargetType="{x:Type TabItem}">
            <!--<Setter Property="Background"
            Value="#7bbfea" />
    <Setter Property="Foreground"
            Value="Black" />-->
            <Setter Property="FontSize"
                    Value="18" />
            <!--<Setter Property="Width"
            Value="120" />-->
    <!--<Setter Property="Height"
            Value="40" />-->
            <Setter Property="IsSelected"
                    Value="{Binding IsSelected,Mode=TwoWay}" />
            <!--<Setter Property="HorizontalAlignment"
                    Value="Stretch" />
            <Setter Property="VerticalAlignment"
                    Value="Stretch" />-->
            <Setter Property="HorizontalContentAlignment"
                    Value="Stretch" />
            <Setter Property="VerticalContentAlignment"
                    Value="Stretch" />
            <Setter Property="BorderThickness"
                    Value="2" />
            <Style.Triggers>
                <MultiTrigger>
                    <MultiTrigger.Conditions>
                        <Condition Property="IsSelected"
                                   Value="True" />
                        <Condition Property="Selector.IsSelectionActive"
                                   Value="True" />
                    </MultiTrigger.Conditions>
                    <Setter Property="Foreground"
                            Value="Black" />
                    <!--<Setter Property="FontSize"
                          Value="20" />-->
                </MultiTrigger>
                <MultiTrigger>
                    <MultiTrigger.Conditions>
                        <Condition Property="IsSelected"
                                   Value="True" />
                        <Condition Property="Selector.IsSelectionActive"
                                   Value="False" />
                    </MultiTrigger.Conditions>
                    <Setter Property="Foreground"
                            Value="Black" />
                    <!--<Setter Property="FontSize"
                          Value="18" />-->
                </MultiTrigger>
            </Style.Triggers>
        </Style>

        <DataTemplate x:Key="tabHeaderDataTemplate"
                      DataType="{x:Type viewmodels:TabItemViewModel}">
            <views:TabItemHeaderView />
        </DataTemplate>

        <DataTemplate x:Key="tabDataTemplate"
                      DataType="{x:Type viewmodels:TabItemViewModel}">
            <views:TabItemView/>
        </DataTemplate>
    </UserControl.Resources>

    <TabControl IsSynchronizedWithCurrentItem="True"
                TabStripPlacement="{Binding Path=TabStripPlacement,Mode=OneWay}"
                ItemsSource="{Binding Source={StaticResource tabSource}}"
                ItemContainerStyle="{StaticResource tabItemStyle}"
                ItemTemplate="{StaticResource tabHeaderDataTemplate}"
                ContentTemplate="{StaticResource tabDataTemplate}"
                SelectedItem="{Binding Path=SelectedTabItem,Mode=TwoWay}"
                Margin="4">
    </TabControl>
</UserControl>
