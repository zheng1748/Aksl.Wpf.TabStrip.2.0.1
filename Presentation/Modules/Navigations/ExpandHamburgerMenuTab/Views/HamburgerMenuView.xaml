<UserControl x:Class="Aksl.Modules.ExpandHamburgerMenuTab.Views.HamburgerMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:views="clr-namespace:Aksl.Modules.ExpandHamburgerMenuTab.Views"
             xmlns:viewmodels="clr-namespace:Aksl.Modules.ExpandHamburgerMenuTab.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <UserControl.Resources>

        <CollectionViewSource x:Key="hamburgerMenuSource"
                              Source="{Binding Path=HamburgerMenuItems}" />

        <DataTemplate x:Key="hamburgerMenuDataTemplate"
                      DataType="{x:Type viewmodels:HamburgerMenuItemViewModel}">
            <views:HamburgerMenuItemView />
        </DataTemplate>

        <Style x:Key="hamburgerMenuListViewItemStyle"
               TargetType="{x:Type ListViewItem}">
            <Setter Property="IsSelected"
                    Value="{Binding IsSelected,Mode=TwoWay}" />
            <Setter Property="FontSize"
                    Value="16" />
            <Setter Property="Margin"
                    Value="0,0,0,0" />
            <Style.Triggers>
                <!--<Trigger Property="IsMouseOver"
                         Value="True">
                    <Setter Property="Background"
                            Value="Honeydew" />
                </Trigger>-->
                <!--<DataTrigger Binding="{Binding Path=IsSelected}">
                    <Setter Property="Background"
                            Value="Red" />
                </DataTrigger>-->
                <MultiTrigger>
                    <MultiTrigger.Conditions>
                        <Condition Property="IsSelected"
                                   Value="True" />
                        <Condition Property="Selector.IsSelectionActive"
                                   Value="True" />
                    </MultiTrigger.Conditions>
                    <!--<Setter Property="Background"
                            Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}" />
                    <Setter Property="Foreground"
                            Value="{DynamicResource {x:Static SystemColors.HighlightTextBrushKey}}" />-->
                    <Setter Property="Foreground"
                            Value="Black" />
                    <Setter Property="FontSize"
                            Value="16.3" />
                </MultiTrigger>
                <MultiTrigger>
                    <MultiTrigger.Conditions>
                        <Condition Property="IsSelected"
                                   Value="True" />
                        <Condition Property="Selector.IsSelectionActive"
                                   Value="False" />
                    </MultiTrigger.Conditions>
                    <!--<Setter Property="Background"
                            Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}" />
                    <Setter Property="Background"
                            Value="#EEEEEEEE" />-->
                    <Setter Property="Foreground"
                            Value="Black" />
                    <Setter Property="FontSize"
                            Value="15.5" />
                </MultiTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <ListView x:Name="hamburgerMenuListView"
              ItemsSource="{Binding Source={StaticResource hamburgerMenuSource}}"
              ItemContainerStyle="{StaticResource hamburgerMenuListViewItemStyle}"
              ItemTemplate="{Binding Source={StaticResource hamburgerMenuDataTemplate}}"
              SelectionMode="Single"
              SelectedItem="{Binding Path=SelectedHamburgerMenuItem,Mode=TwoWay}"
              Width="320">
    </ListView>
</UserControl>
